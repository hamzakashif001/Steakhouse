# Copy to ".env" and fill in to enable real reservation submissions.
# Create a free form at https://formspree.io and paste its form id (the part
# after /f/ in the endpoint, e.g. "xmyzabcd"). Leave unset for showcase mode
# (the form validates and shows a success state without sending anything).
VITE_FORMSPREE_ID=
